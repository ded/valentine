var v = require('valentine')
ender.ender(v)
ender.ender({
    merge: v.merge
  , extend: v.extend
  , each: v.each
  , map: v.map
  , toArray: v.toArray
  , keys: v.keys
  , values: v.values
  , trim: v.trim
  , bind: v.bind
  , parallel: v.parallel
  , waterfall: v.waterfall
  , inArray: v.inArray
  , queue: v.queue
})